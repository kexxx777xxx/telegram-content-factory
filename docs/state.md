# Стан системи

Знімок на **2026-08-24**. Перезаписується при зміні того, що тут написано —
це не історія (вона в `worklog/` і `CHANGELOG.md`), а відповідь на питання
«в якому стані все зараз».

## Одним абзацом

Мультипроєктна система веде кілька Telegram-каналів: у кожного власний банк тем,
голос, розклад, ланцюжок моделей і промпти. Цикл **тема → текст → зображення →
публікація** проходить сам, без участі людини. Усі 10 фаз реалізовано; 137 тестів,
`typecheck` і `build` чисті.

## Що працює

| Частина | Стан | Де |
| --- | --- | --- |
| Розклад | працює, з таймзонами й DST; відповідає лише на питання «чи час говорити» | `scheduler/slots.ts` |
| Черга публікацій | працює: закріплений час → пріоритет → готове раніше → випадково (ADR 0009) | `services/publishNow.ts` |
| Наповнення буфера | працює: планувальник тримає `posts_buffer` постів і генерує одразу | `scheduler/planner.ts` |
| Черга в Postgres | працює, `SKIP LOCKED`, дедуп, backoff, reaper | `queue/` |
| Ланцюжки моделей | працює, з обходом 429 і circuit breaker | `ai/chain.ts` |
| Банк тем із дедупом | працює, лексичний + семантичний | `services/ideas.ts` |
| SVG-двигун | працює, з санітайзером і детермінованим fallback | `media/svg/` |
| Публікація в Telegram | працює, з відновленням після обриву | `telegram/publisher.ts` |
| Формат поста | ліміт довжини і хештеги форсуються кодом, а не промптом (ADR 0008) | `telegram/html.ts`, `services/publish.ts` |
| Дзеркала каналів | працює: `copyMessage` після публікації, best-effort | `services/publish.ts` |
| Ручний запуск | працює для поста, проєкту і теми | `services/publishNow.ts` |
| Адмінка | працює, auth + CSRF + шифровані секрети; вкладки й редагування ключа мають власні URL | `apps/web/` |
| Керування чергою | працює: проєкт і час у рядку, видалення поштучно й цілим статусом | `api/routes/jobs.ts` |
| Глобальні налаштування | працює: стиль ілюстрацій за замовчуванням | `services/settings.ts` |
| Batch-тариф Gemini | працює для тем, тексту й ілюстрації; одне замовлення на кілька постів; режим на проєкт | `ai/batch.ts` |
| Журнал | працює; фільтри за моделлю і тарифом (batch / звичайний виклик) | `apps/web/…/ProjectJournal.tsx` |
| Бекап конфігурації | працює, gzip-JSON у адмін-чат раз на добу | `services/backup.ts` |

## Що НЕ перевірено на живих інтеграціях

Тестами не замінюється — потрібні справжні токени й канал:

- успішний `sendPhoto` у реальний канал;
- `copyMessage` у дзеркальний канал (на підробному Bot API покрито);
- відновлення публікації після обриву **на живому** Bot API (на підробному покрито);
- інтерактив адмін-бота (кнопки апруву);
- гілка image-моделі (генерація зображення моделлю, не SVG).

Усі шляхи навколо них покриті, включно з обробкою помилок Telegram.

## Знані обмеження

- **Batch працює для тем, тексту і зображення.** Не батчиться складання опису
  картинки (`image_prompt`) — це короткий текстовий виклик, і друга черга через
  нього подвоїла б очікування заради копійок. Ремонт SVG теж синхронний: він
  працює над уже отриманою відповіддю.
- **Замовлення збирається щонайменше з двох запитів.** У batch платять за
  запит, тож економія від одного така сама, як від двадцяти, — а чекати треба
  однаково. Один кандидат у буфері означає, що буфер замалий для дешевого
  тарифу: пост іде звичайним викликом (у режимі «лише batch» — лишається
  чекати). Стеля пачки — 20 постів, для малювання 5, бо кожному сусідові
  спершу треба скласти опис картинки окремим викликом.
- **Право на batch — властивість джоби, не запасу часу.** Буферна
  `generate_post` чекає добу, `generate_and_publish` (ручний запуск, закріплена
  хвилина, JIT) — ні. Порогів «скільки лишилось до слоту» більше немає разом зі
  слотами (ADR 0009); лишився дедлайн замовлення, після якого воно
  скасовується і крок іде звичайним викликом.
- **Дзеркало не гарантоване.** Копія йде після коміту публікації і не повторюється: помилка
  пишеться в журнал проєкту, пост лишається опублікованим в основному каналі. Ретрай тут означав би
  ретрай уже зробленої публікації. Перевірка каналу («Перевірити Telegram») теж стосується лише
  основного каналу.
- **Перебору ключів немає** — навмисно. Вичерпаний ключ веде до наступної
  **моделі** ланцюжка, а не до сусіднього ключа. Див. ADR 0003.
- **Сесія не відкликається поштучно.** Токен самодостатній; єдиний спосіб
  розлогінити — змінити `ADMIN_PASSWORD_HASH` або `SESSION_SECRET`, і це
  розлогінює всіх.
- **Один процес** тримає HTTP, планувальник і воркери. Другий інстанс безпечний
  (advisory-лок + `posts_slot_uniq`), але не перевірявся в бою.
- **Абзац без жодного пробілу**, довший за 4096 символів, публікацію завалить —
  розбити його безпечно неможливо, тож джоба чесно падає.
- **Текст понад «Довжину поста» відхиляється цілком.** Він не зберігається, пост
  лишається `planned` з поясненням у `error`, і черга пише текст заново;
  ілюстрація при цьому не перемальовується. Наслідок, який видно в експлуатації:
  зменшення ліміту робить уже готові довші пости непублікованими, доки їх не
  перегенерують. Раніше це коштувало слотів — тепер такий пост просто стоїть у
  черзі далі.
- **Точний час публікації не видно наперед.** До слоту відомо лише «наступний
  о 12:00», а не «о 12:00 вийде ось цей пост»: черга обирає пост у момент
  слоту. Для конкретного поста це вирішує закріплення часу.
- **Статус `skipped` лишився в енумі як історія.** Нічого його більше не
  створює; прибрати значення з Postgres-енуму — це перезапис таблиці.
- **Хештеги моделі зрізаються лише хвостом.** `stripTrailingHashtags` знімає
  хвіст із голих тегів у кінці; теги, обгорнуті розміткою (`<i>#тег</i>`) або
  посеред абзацу, лишаються в тексті поруч із тими, що допише код.

## Відкрите

- ESLint і Prettier не налаштовані: `npm run typecheck` — єдина статична
  перевірка. Потрібне рішення про конфіг.
- `npm audit`: `react-router` 7.12–8.2, high (CSRF bypass **у RSC-режимі**). Тут
  SPA без RSC, не експлуатується; `audit fix --force` відкочує на 7.11 і ламає API.

## Перевірка стану

```bash
docker compose up -d db && npm run db:migrate
npm run typecheck && npm test && npm run build
```

CI ганяє те саме на кожен push і PR — `.github/workflows/ci.yml`.
